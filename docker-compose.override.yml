services:
  embedding:
    image: vara-infra-embedding
    expose:
      - "9000"
    volumes:
      - hf-cache:/cache/hf
    restart: unless-stopped

  backend:
    environment:
      EMBEDDING_SERVER_URL: http://embedding:9000/embed
    depends_on:
      - embedding

volumes:
  hf-cache:
