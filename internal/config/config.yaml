# VARA backend config sample
# 실제 값은 환경변수(.env)에서 주입되며 본 파일은 viper 도입 시 기본값/문서 용도로 사용한다.

server:
  port: 8080

postgres:
  host: localhost
  port: 5432
  user: vara
  password: changeme
  dbname: vara
  sslmode: disable

redis:
  addr: localhost:6379
  password: ""
  db: 0
